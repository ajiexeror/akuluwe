# * * * * * root echo "Hello world" >> /var/log/cron.log 2>&

# lupa absen pulang # setiap hari jam 23:58
58 23 * * * curl --insecure https://localhost/cron/attendance_lupa_pulang

# Don't remove the empty line at the end of this file. It is required to run the cron job
